# CapiMax PropShare — Build Progress (state for resumption / post-compaction)

**Last updated:** 2026-06-28 (Phase 15c complete — Investor Communications; build-everything-real direction)

## 🧹 Pre-launch cleanup pass (2026-06-24) — audit ⚠️ items A+B+C+D fixed

A full read-only all-roles dashboard audit flagged presentation/navigation-honesty gaps
(money/transaction paths were confirmed clean). All were fixed under DELETE NOTHING /
no-fabricated-numbers / no-fake-success / server-authoritative-for-money. **Frontend tests
now 29 (was 23)**; ruff/black/mypy unaffected (no backend change); eslint 0 errors / 11
pre-existing warnings; tsc + vite build green.

- **A (mock-as-real):**
  - **Owner overview** (`src/pages/OwnerDashboard.tsx`) — Total Portfolio Value + Total Investors now **derived** from the owner's real properties (Σ `total_value`, Σ `investors_count`); Monthly Revenue + Avg. Occupancy + the revenue chart **honest-disabled** ("Not available yet" — no per-owner rental ledger). Greeting uses the real user name. Old $12.5M/$85k/1,234/94% mocks retired.
  - **Developer dashboard** (`src/pages/DeveloperDashboard.tsx`) — Active Projects / Total Raised / Total Investors / Avg Funding **derived** from real projects; Avg. Investment derived (Σ raised ÷ Σ investors); Funding tab Total Raised + Active Funding Goals **derived**. Monthly funding chart, "This Month", Repeat Investors, **Milestones** tab, **Investor Communications** **honest-disabled** (no backend). Old $45.2M/3,456/92% + Creek-Harbour milestone/comms arrays retired.
  - **Investor Wallet** (`src/components/dashboard/InvestorWallet.tsx`) — fake saved "Payment Methods" (Emirates NBD/Visa/Bitcoin) replaced with an **honest empty-state**; add-method button disabled. (Money page — no fake cards.)
  - **Investment Certificates** (`src/components/dashboard/InvestmentCertificates.tsx`) — whole surface **honest-disabled** (documents deferred); mock CERT-2024-* + dead View/Download removed; component kept.
  - **Family estate/gifting** (`src/components/dashboard/FamilyInvestment.tsx`) — Beneficiaries & Gifting sub-tabs **honest-disabled pending their own (final) phase**. **CORRECTION (owner, 2026-06-24): estate/beneficiary/gifting IS CapiMax's own scope — NOT BRX.** `FamilyBeneficiaryGifting.tsx` (mock) **kept in repo, not deleted** (DELETE NOTHING); the real backend will be built in the **last** group (inheritance engine, with a legal-design step). Interim disabled copy corrected to "coming soon on CapiMax" (no longer claims BRX / out-of-scope).
  - **Public homepage Secondary section** (`src/components/home/SecondaryMarket.tsx`) — fake "live" listings + "$45,000 traded/24h" replaced with an honest explainer + real "View All"/CTA links (listings API is auth-only; no fake data to guests).
- **B (fake-success / false claims):** Support (`src/pages/Support.tsx`) — contact form now opens a **real mailto** to support (no fake "confirmation email sent"); live chat gives an **honest auto-reply** funnelling to WhatsApp/email (no simulated agent); "Online Now"/"replies instantly" → honest copy.
- **C (dead controls):** Owner/Developer header Bell (hardcoded 5/8 removed) + Settings now navigate; Owner "List Property" opens its tab; Developer "Add Milestone Update"/"Send Update" disabled; global header (`src/components/layout/MainLayout.tsx`) User icon → `/settings`, **search wired** → `/marketplace?q=` (Marketplace seeds search from `?q=`); info-page Download/View-Report/View-Profile buttons honest-disabled (SamplePropertyDetails / AdvancedPropertyPage / PropertyDetails).
- **D (404s):** Footer (`src/components/layout/Footer.tsx`) dead links repointed to real routes (Careers/Press/redundant About removed); landing "Reinvest Now"/"Manage Family" → `/dashboard?tab=…`; Auth (`src/pages/Auth.tsx`) Terms/Privacy/Risk-Disclosure now open the real legal routes; unwired "Remember me" removed.
- **E (confirm-by-design):** secondary-listing seller price stays **server-clamped** in `secondary_service.create_listing` (`price_min_pct`/`price_max_pct` → 422 `PRICE_OUT_OF_BOUNDS`) with the buyer resale fee + fee-rate recomputed server-side. No change.
- **Tests added:** `OwnerDashboard.test.tsx`, `DeveloperDashboard.test.tsx` (derived-stat real-data + honest-disabled + mock-retired assertions), `HonestDisabled.cleanup.test.tsx` (certificates + wallet payment-methods empty-state).
- **Still owner-owned (NOT changed this pass — flag for compliance before public launch):** landing marketing figures — homepage `$50M AUM` / `15,000 owners` / `Regulated by FSA`, and **`LiquiditySection` `$12.5M+ Total Liquidity Provided` + `Up to 12% APY`** (PASSIVE is hard-locked; treat the APY teaser with care). Consistent with the standing "owner asserts substantiation; confirm before launch" decision.

## 🔁 GOVERNING DIRECTION (owner, 2026-06-24) — build REAL backends for ALL disabled/mock surfaces

**Honest-disable is NO LONGER an acceptable END-STATE.** The owner has decided that every
previously-disabled / mock / "coming soon" surface gets a **real backend**, built properly,
**one group at a time** (investigate → design → build) — nothing deleted, nothing left
permanently disabled. This **strengthens DELETE NOTHING**: don't silent-remove AND don't ship
a permanent honest-disabled stub — wire it to a real backend.
**DEPLOYMENT IS DEFERRED** until this whole plan finishes (the VPS-verification stack + 6 crons
stay gated behind `plan/DEPLOYMENT_CHECKLIST.md`; deployment happens AFTER, not before). Owner
accepts the extended timeline and has been advised of the legal (inheritance) + security
(payment-method storage) risks. The money/transaction core (0–14) stays server-authoritative,
atomic, idempotent, signed-webhook-sourced, append-only-ledger+audit, reconciled — unchanged.

**Build-everything-real plan — group order (one at a time):**
1. ✅ **15 — Owner/Developer stats — DONE** (2026-06-24). Two read endpoints (`GET /api/v1/owner/portfolio-stats`, `GET /api/v1/owner/funding-stats`, OwnerDep-gated) + `owner_stats_service` + schemas, **no migration**, pure aggregation. portfolio value = Σ owner `total_value`; total investors = distinct net-holders (`HAVING SUM(units)>0`); monthly revenue = completed-distribution `gross_pool` by UTC month (current-month card + 6-month chart, **real zeros**) + per-property revenue; developer funding = confirmed-investment `amount` by UTC month + this-month; repeat investors = ≥2 confirmed ÷ distinct. **Occupancy = honest empty** (`null` → "No occupancy data yet"; card kept — DELETE NOTHING — no fabricated 94%). Frontend: `ownerStatsApi`, all owner+developer cards wired, **real recharts charts re-added** (fed by the real monthly series w/ real zeros), **"Emaar Properties" greeting retired**, DeveloperDashboard gained `?tab=` deep-link; Milestones + Investor Communications show honest **"coming soon — being built"**. Tests: `test_owner_stats_db.py` (+10), `OwnerDashboard.test.tsx`/`DeveloperDashboard.test.tsx` (+3). **Design:** `plan/phase-15-stats.md`. ⚠️ Restart the backend (new `owner_stats` router) before browser re-testing.
2. ✅ **15b — Developer Milestones — DONE** (2026-06-25). Migration **`0015_phase15b_milestones`** (down_revision 0014): `milestone_status` enum (`planned|in_progress|completed`) + `property_milestones` table (`property_id` FK CASCADE, title, description, status, `progress_pct` 0-100 CHECK, **`value_index` nullable = NAV step** [null for regular milestones, set for construction], `target_date`, `completed_at`, `sort_index`, `created_by`, timestamps; `(property_id,sort_index)` index) + a **deterministic, idempotent backfill** of the legacy `content.timeline[]` JSONB (relative dates → concrete, anchored on `properties.created_at`; done/active/upcoming → completed/in_progress/planned; `valueIndex`→`value_index`; array order → `sort_index`; created_by = owner). **JSONB kept** (superseded, not deleted). Backend: `milestone_service` (owner-scoped CRUD + reorder + **pure `timeline_to_milestone_rows` converter** shared by migration/seed/tests + `construction_progress_from_rows`/`_map`), `routes/milestones.py` (5 owner-scoped endpoints under `/api/v1/owner/properties/{id}/milestones`, OwnerDep + `property.owner_id` → **403 NOT_PROPERTY_OWNER**), schemas; **public read embeds `milestones[]` + `construction_progress` in the existing `GET /properties/{id_or_slug}`** (no new public path); `my_properties` gains `construction_progress` (one-query map, no N+1). **constructionProgress rule = current milestone's progress** (`in_progress` %, else 0 — avoids the admin-step skew). Editable SQLAdmin `PropertyMilestone` view. **Idempotent seed** (milestones only if a property has none). Frontend (DELETE NOTHING — components kept, data sources swapped): **`PropertyTimeline` is now prop-driven from real milestones — the fully-fake 8-event mock is deleted** (the key audit-missed fix); `toSampleProperty.timeline` built from `d.milestones` (surfaces C/D keep NAV via `value_index`); construction bars fed by computed `construction_progress`; the dashboard **Milestones tab is a real CRUD surface** (`MilestonesManager`: project selector + list + add/edit/delete/reorder, **"Add Milestone Update" enabled** — "coming soon" retired); `ownerMilestonesApi`. Tests: `test_milestones_db.py` (+10), `PropertyTimeline.test.tsx`/`properties.milestones.test.ts`/updated `DeveloperDashboard.test.tsx` (+4 net). **Backfill verified live: 27 rows / 7 properties; demo `construction_progress`=25.** ⚠️ Run `alembic upgrade head` (→0015, runs the backfill) + restart the backend before browser re-testing.
3. ✅ **15c — Investor Communications — DONE** (2026-06-28). Migration **`0016_phase15c_investor_updates`** (down_revision 0015, **no enum change**): `developer_updates` (property_id FK CASCADE, created_by, subject, body, `recipient_count` snapshot, created_at) + `developer_update_recipients` (update_id FK, user_id FK, **`notification_id` FK→notifications** for real read-count, **UNIQUE(update_id,user_id)** → idempotent per recipient). **Audience = per-property net-holders** (distinct `ownership_ledger` Σ units>0 — same as distribution; excludes fully-exited, includes secondary buyers with no investment row). **Reuses Phase-12 `notify()` + `email_outbox` 1:1** — the only seam change: **`notify()` now RETURNS the created `Notification`** (id generated client-side, no extra flush; purely additive — all 10 callers invoke it as a statement, audited, none consumed the old `None`). Send = create update → fan out to net-holders (in-app always; email when `investment_updates` pref on) → record a recipient row per holder linked to its notification → snapshot `recipient_count`; one atomic tx. **Metrics counts-only & real:** `recipient_count` (snapshot) + `read_count` (recipients JOIN notifications.read). **NO open/click/delivered** (no infra — intentionally absent, never fabricated). Backend: `developer_update_service` + `routes/developer_updates.py` (`POST /api/v1/owner/properties/{id}/updates`, `GET /api/v1/owner/updates[?property_id=]`, OwnerDep + `property.owner_id` → **403 NOT_PROPERTY_OWNER**); investors READ via the existing `GET /notifications` feed + email (no new investor endpoint); read-only SQLAdmin `DeveloperUpdate`/`DeveloperUpdateRecipient`. Frontend: `ownerUpdatesApi`; **`InvestorCommunications`** (composer + sent-history with real recipient/read counts — "coming soon" retired, "Send Update" enabled); **15b bridge = MANUAL** "Notify investors" per-milestone action in `MilestonesManager` that **pre-fills** an update for the developer to review + send (**never auto-sends**). Tests: `test_developer_updates_db.py` (+10, incl. notify()-returns-id), `InvestorCommunications.test.tsx` (+2), updated `DeveloperDashboard.test.tsx`. ⚠️ Run `alembic upgrade head` (→0016) + restart the backend before browser re-testing. **Deferred:** email open/click/delivered tracking (needs provider webhooks/pixel); a property-page "Updates" feed (investors use the notifications bell + email).
4. ✅ **Storage → Documents / Certificates — DONE** (2026-06-28). **No migration** (documents/avatar/images columns exist — migrations stay at **0016**). **Storage seam** `integrations/storage.py` (`STORAGE_PROVIDER` local-FS dev / s3 prod, lazy boto3) — real files, mirrors the email seam. **Documents:** owner-scoped upload + PUBLIC list/download (`document_service`, `routes/documents.py`; property docs public on active/funded, draft→404, user-scoped not public-served; **403 NOT_PROPERTY_OWNER**). **Certificates:** live dependency-free PDF from the caller's net `ownership_ledger` holding (`GET /investments/certificate/{id}`, 404 NO_HOLDING; factual wording). **Unblocked seams:** `POST /properties/{id}/images` (was 503) + `POST /profiles/me/avatar` store real files; public assets served inline via `GET /files/{key}` (allow-listed prefixes). Read-only SQLAdmin `Document` view. Frontend: `documentsApi`/`certificateApi`, `apiRequest` FormData support + `fetchBlob`; **PropertyDocuments + InvestmentCertificates wired to real data** (mock lists deleted, "not available" retired). Tests: `test_documents_db.py` (+11), `PropertyDocuments.test.tsx` + updated cleanup test (+3 frontend). ⚠️ No migration; restart the backend (new routers) before browser re-testing.
5. ✅ **Saved Payment Methods — DONE** (2026-06-28). Migration **`0017_group3_payment_methods`** (down_revision 0016): `payment_customers` (one Stripe customer per user) + `saved_payment_methods` (**TOKEN** `provider_payment_method_id` + safe metadata brand/last4/exp + `is_default`; **UNIQUE(provider, pm token)** → idempotent re-add). **PCI-safe: NEVER stores raw card data (PAN/CVC).** Card collected client-side via a Stripe **SetupIntent** (Stripe.js/Elements — raw card never reaches our server); backend fetches brand/last4/exp **server-side** from Stripe. Reuses the Phase-4 Stripe gateway seam (mockable; **honest 503 when unconfigured**). Backend: `payment_method_service` + `routes/payment_methods.py` (`/api/v1/wallet/payment-methods` list/setup-intent/add/delete/set-default, PrincipalDep, user-scoped — cross-user add→403, foreign delete→404; first method = default, delete reassigns; **API never returns the token**); read-only SQLAdmin views. Frontend: `paymentMethodsApi`; **InvestorWallet payment-methods is a real vault** (list + set-default + remove; Add starts the SetupIntent). Tests: `test_payment_methods_db.py` (+9), updated cleanup test (+1 frontend). ⚠️ Run `alembic upgrade head` (→0017) + restart the backend. **Deferred (prod):** mount Stripe Elements card-entry widget + set `STRIPE_*` keys (vault management + SetupIntent are real/tested; card-entry is config-gated like the deposit/withdrawal rails).
6. **Estate / inheritance / beneficiaries / gifting — LAST** — **CapiMax's OWN scope** (corrected — NOT BRX), with a **dedicated legal-design step BEFORE any code**: death/inactivity verification must be **server-trusted** (never client-asserted); **Islamic-inheritance / forced-heirship rules conflict with free allocation**; the existing **"legally acknowledged / reviewed by legal partners" UI claims must be made real or removed**; **needs a real lawyer**.

**Occupancy / tenancy** stays its OWN separate future domain (a full property-management
subsystem: rentable-spaces + leases + ongoing data entry — physical units ≠ fractional-ownership
units). Currently honest-empty in the Owner dashboard; built ONLY if the owner explicitly scopes it.

**Estate-scope CORRECTION:** estate/beneficiary/gifting is CapiMax's own feature, NOT handed to
BRX (prior notes were wrong) — keep the UI, build it real in group 6. (The "BRX" 8000/8080 note
elsewhere is only about avoiding local port collisions with a *different* local project.)

## 🟡 Still-open BUSINESS decisions (unchanged, NOT forgotten — owner-owed)

- **PASSIVE LP economics** — yield-funding source + reserve buffer + written ALM rules + capital/reserve adequacy + **FSA license** (with counsel). Engine BUILT but **hard-locked** (`lp_passive_enabled=false`); no real deposit possible; fixed APY must never render as guaranteed. (See PASSIVE-deferred section below.)
- **Installment-plan economics** — recurring scheduled payments; `InstallmentCalculator` commit CTA stays honest-disabled until designed. (Will become a build group under the "build-everything-real" plan.)
- **Public marketing claims (owner asserts substantiation; CONFIRM REAL before public launch — esp. regulatory + the APY-on-hard-locked-PASSIVE):** homepage `$50M+ AUM` / `15,000+ owners` / `Regulated by Financial Services Authority`; `LiquiditySection` `$12.5M+ Total Liquidity Provided` + **`Up to 12% APY`** (PASSIVE is hard-locked — this teaser is high-risk). Left as-is in code per the standing owner decision; flagged for compliance.

**Phases complete & verified:** 0, 1, 2, 3, 3.5, 4 (Stripe deposit live-tested), 5 (wallet-invest live-tested), 6 (returns/distributions), 7 (withdrawals — Stripe Connect + NOWPayments, mocked-provider tests), 8 (secondary market), **9 ACTIVE** (LP buyback — live-tested end-to-end via seed accounts; mgmt-fee leak fix verified), **10** (Family Groups & Gifting — real/pending model, live-testable), **11** (Broker referrals & commissions — revenue-based, capped, admin-rate), **12** (Notifications + email: in-app read path + transactional email outbox), **13** (Admin/cron/reconciliation/hardening), **14** (Integration & Launch — real reinvest discount, owner wallet wired, last mocks honest-disabled, deployment checklist). **Tests: 272 backend / 42 frontend** (Group-3 payment-methods +9 backend / +1 frontend; Group-2 storage/docs +11/+3; 15c +10/+2; 15b +10/+4; 15 +10/+3; cleanup +6 frontend). **Migrations at 0017** (Group 3). Phase 9 **PASSIVE deferred + hard-locked**. **Phases 0–14 COMPLETE + green** and the money/transaction core is server-authoritative + audited — BUT per the GOVERNING DIRECTION above, work is **NOT** done: we are building real backends for ALL previously-disabled/mock surfaces (15 ✅ → 15b ✅ → 15c ✅ → storage/certificates ✅ → payment methods ✅ → **estate (DESIGN ONLY — last)**, one at a time). **Deployment is DEFERRED until that plan finishes** (`plan/DEPLOYMENT_CHECKLIST.md` stays gated). Demo seed: `backend/scripts/seed_phase9_demo.py`.

## ✅ Final phase roster (all 0–14 COMPLETE, tested, green)

Every phase was **built to FIT the existing frontend** (wiring mock→real data, never a redesign) with the **money-safety rules** (server-authoritative, atomic, idempotent, signed-webhook-sourced, append-only ledger + audit) applied to every financial path:

- **0 Foundation** — FastAPI + async SQLAlchemy + Alembic + CI; `/healthz`, `0001` baseline schema.
- **1 Identity/Auth/RBAC** — app-owned users, Argon2, JWT + refresh cookie, Scenario-B multi-role; Supabase removed from the SPA.
- **2 KYC (Sumsub)** — signed idempotent webhook flips status, no human step; honest 503 until configured.
- **3 Property catalog & marketplace** — live data; all hardcoded property arrays retired.
- **3.5 Hardening/admin** — DB-backed test harness + SQLAdmin (money tables read-only).
- **4 Wallet & deposits (Stripe + NOWPayments)** — credit only on signed webhook; two-layer idempotency; Stripe live-tested locally.
- **5 Investment engine** — wallet + direct-pay rails, oversell-safe, reservation lifecycle; fees admin-configurable.
- **6 Returns & distributions** — Hamilton pro-rata exact-to-cent, mgmt fee, atomic, idempotent per period.
- **7 Withdrawals** — hold-on-request, Stripe Connect (bank) + NOWPayments (crypto), $5000 admin auto-limit.
- **8 Secondary market** — atomic investor-to-investor resale; lock order listing→property→wallets.
- **9 Liquidity Provider** — **ACTIVE** buyback live-tested; **PASSIVE** built but **hard-locked OFF**; never-commingle invariant.
- **10 Family groups & gifting** — real on-ledger REAL/PENDING model, materialize-on-KYC, 7.5% family discount.
- **11 Broker referrals & commissions** — % of platform revenue (capped by DB CHECK), admin-rate, signup-only link.
- **12 Notifications + email** — in-app read path + transactional email outbox (cron-drained); channels = in-app + email only.
- **13 Admin/cron/reconciliation/hardening** — cron-auth seam, LP-expiry sweep, DB-wide reconciliation report, settings validation, rate-limiting, optional-Redis health.
- **14 Integration & Launch** — real server-side reinvest discount, owner wallet wired, last mocks honest-disabled, `DEPLOYMENT_CHECKLIST.md`.

**BUILD IS DONE.** Remaining work: (a) owner-gated **live deployment** (`plan/DEPLOYMENT_CHECKLIST.md`); (b) **final all-roles dashboard audit** — a read-only, element-by-element inventory across every role to catch any last dead/fake element before launch (in progress, requested next).

## 🔒 Key cross-phase invariants (NEVER break)

- **Global lock hierarchy** across Phases 5/6/7/8/9/10: **market-record (listing / exit-request) → property → wallets (sorted by `str(user_id)`)**. Wallet-only paths (P7 withdrawals, P10 reinvest/allocate) lock just the wallet; never inverted.
- **Shared `secondary_service.reserved_units(user, property)`** is the single reservation authority: a unit can't be simultaneously secondary-listed (P8), LP-exit-requested (P9), **or** family-allocated-pending (P10). Checked under the property `FOR UPDATE` at every create.
- **`ownership_ledger` is the single source of truth for holdings** (never trust a position/allocation cache); each acquisition row carries a **stamped `fee_rate`** (Decision-2) so Phase-6 mgmt fee is ownership-derived and never retroactively changed.
- **Money rules:** server-authoritative amounts, single atomic tx, idempotent (Idempotency-Key + UNIQUE), append-only `transactions` ledger + `audit_log`, signed webhooks are source of truth (never browser redirects), hold-on-request for money-out. Invariant `balance == SUM(ledger)` asserted throughout.

This file preserves working state across context compaction. Read it first when resuming.

---

## Phases complete

- **Phase 0 — Foundation** ✅ FastAPI service in `backend/`, async SQLAlchemy, Alembic, CI (`backend/.github/workflows/ci.yml`: lint-test + migrate-schema on `postgres:16`), `/healthz` + `/openapi.json`. Migration `0001_initial` builds the baseline schema from scratch (portability preamble stubs the old Supabase `auth`/`storage` objects only if absent). Gate: `alembic upgrade head` clean on fresh Postgres.
- **Phase 1 — Identity, Auth & Access Control** ✅ FULL CUTOVER DONE.
  - Backend: app-owned `users` (+ `oauth_identities`, `refresh_tokens`, `email_tokens`, `role_grant_requests`); migration `0002_phase1_identity` re-points all FKs off the `auth.users` stub onto `public.users` and drops the provisioning trigger (clean cutover, no real users). Argon2 passwords, JWT access token (15 min, in-memory) + httpOnly/Secure/SameSite refresh cookie with rotation. Endpoints: register/login/oauth/refresh/logout/me/switch-role/roles-request/verify-email/password(forgot,reset,change)/admin(users,roles,role-requests). Scenario B multi-role: JWT carries `roles` + `active_role`; `require_active_role_db` re-checks `user_roles` at action time (revoked role can't act within token TTL). `scripts/seed_admin.py` seeds first admin.
  - Frontend: `supabase-js` REMOVED entirely (deleted `src/integrations/supabase/*` and `src/integrations/lovable/*`, removed from `package.json`). New `src/lib/api.ts` (in-memory access token + cookie refresh, one-shot 401 retry). `AuthContext` rewritten (no client role mutation). `ProtectedRoute` guards in `App.tsx`. Role-switcher repurposed (backend-authorized roles only, server-enforced). `Auth.tsx`/`AccountSettings.tsx` wired; OAuth initiation + `AuthCallback.tsx`. Frontend `.env`/`.env.example` use `VITE_API_BASE_URL`, `VITE_GOOGLE_CLIENT_ID`, `VITE_APPLE_CLIENT_ID`.
- **Phase 2 — KYC Automation & Gating (Sumsub)** ✅
  - `services/integrations/kyc_sumsub.py` (signing, `verify_payload`, `map_review_result`), `services/kyc_service.py` (start + idempotent webhook + admin exception path), `services/notification_service.py` (`notify`), `core/audit.py` (`write_audit`). Migration `0003_phase2_kyc` adds provider columns + `manual_review_required` to `kyc_verifications`, plus `kyc_webhook_events` (idempotency) and `audit_log` (append-only). Routes: `GET /api/v1/kyc/me`, `POST /api/v1/kyc/me/start`, `POST /api/v1/kyc/webhook/sumsub` (signature-verified, idempotent on payload digest, flips status with NO human step), admin `GET /api/v1/admin/kyc` + `POST /api/v1/admin/kyc/{user_id}/decision`. `require_kyc_verified` gate is live for Phase 5+. Honest 503 when Sumsub unconfigured. Frontend `KYCVerification.tsx` launches the Sumsub WebSDK (CDN) + polls status; upload-only stub retired.
- **Phase 3 — Property Catalog & Live Marketplace** ✅
  - Backend: migration `0004_phase3_property_catalog` adds catalog columns to `properties` (`slug` unique, `model`, `subtitle`, `country`, `city`, `expected_yield`, `capital_appreciation`, `total_return`, `funding_progress`, `investors_count`, JSONB `content`) + status/model/owner/slug indexes. `Property` ORM updated. `schemas/property.py`, `services/property_service.py` (public list w/ filters+sort+pagination — only `active`/`funded`; public detail by id-or-slug; owner create→draft / update (draft+under_review only) / submit→under_review / list-owner; admin approve→active / reject→draft / close→closed, all audit-logged). Routes `api/routes/properties.py` (`GET /properties`, `GET /properties/{id_or_slug}`, `POST /properties`, `PATCH /properties/{id}`, `POST /properties/{id}/submit`, `POST /properties/{id}/images` honest-503 storage seam, `GET /owner/properties`) + `api/routes/admin/properties.py` (list/approve/reject/close). Owner writes gated by `require_active_role_db("owner")`; admin by `AdminDep`. `scripts/seed_properties.py` seeds the 7 demo properties as `active` (idempotent by slug).
  - Frontend: `src/lib/api.ts` `propertyApi` (+ DTOs) and `src/lib/properties.ts` adapters (`toMarketplaceProperty`, `toSampleProperty`). Live data via TanStack Query in `Marketplace.tsx`, `PropertyDetails.tsx` (id-or-slug), `SamplePropertyDetails.tsx` (slug), `AdvancedPropertyPage.tsx` + `ConstructionModelPage.tsx` (by model), `FeaturedProperties.tsx`, `DeveloperDashboard.tsx` Projects, `OwnerDashboard.tsx` (live list + shared `PropertyCreationForm`). `PropertyCreationForm.tsx` wired to create→submit. `src/data/sampleProperties.ts` reduced to TYPES only (mock data array + `getSampleBySlug` deleted). All hardcoded property arrays retired (`allProperties`/`sampleProperties` no longer feed any screen).

- **Phase 3.5 — Pre-Phase-4 Hardening & Admin** ✅ (stay on FastAPI; no money features)
  - **DB-backed integration test harness** (`app/tests/conftest.py`): builds a throwaway `<db>_test` database from the real Alembic migrations, overrides `get_session` to it, gives tests an httpx `AsyncClient` + a sync SQL helper. Auto-SKIPS the whole layer if Postgres is unreachable (the DB-free unit tests still run everywhere). New committed gate tests: `test_auth_rbac_db.py` (switch-role→403; register provisions profile+wallet+kyc atomically; full rollback on provision failure; **revoked-role-within-valid-TTL → 403 ROLE_REVOKED**), `test_kyc_webhook_db.py` (GREEN webhook → `verified`; replayed delivery deduped no-op), `test_property_flow_db.py` (create→submit→admin-approve→public visibility; non-owner can't submit; non-admin can't approve; owner can't edit once active), `test_admin_panel_wired.py` (panel gating). **Total tests: 58** (was 46), all green; ruff+black+mypy clean.
  - **Admin panel (SQLAdmin)** at `/admin` (`app/admin.py`): server-side gated by `AdminAuth` — login requires valid credentials AND the `admin` role (verified in DB); anonymous → redirect to `/admin/login`; wrong password → 400, no session. Views: **Property** (with **Approve / Reject / Close** actions calling `property_service.admin_moderate`, audit-logged), **User**, **UserRole** (grant/revoke), **KycVerification** (override). Money tables — **Investment, Wallet, Transaction — are READ-ONLY** in the panel (no create/edit/delete). Deps added: `sqladmin`, `itsdangerous`.

- **Phase 4 — Wallet & Deposits (Stripe + NOWPayments)** ✅ FIRST MONEY PHASE (design signed off first)
  - **Money rules enforced:** balances are server-authoritative; the wallet is credited ONLY by a signature-verified provider webhook (never on browser redirect), with the **provider-captured amount** (not the client's). Every credit is one atomic tx — `SELECT … FOR UPDATE` on the wallet row → `wallets.balance` += → append-only `transactions(type='deposit')` row → `audit_log` row. Invariant `balance == SUM(ledger)` is test-asserted. Amounts are `Decimal`/USD.
  - **Two-layer idempotency** (replay can never double-credit): (1) `payment_events` UNIQUE(provider,event_id) dedupes deliveries; (2) the credit only fires on the `pending→succeeded` transition under the row lock.
  - Backend: `wallet_service` (the ONLY balance mutator), `payment_service` (deposit intents + webhook credit), gateways `stripe_gateway` (Checkout Sessions; `Stripe-Signature` HMAC-SHA256) + `nowpayments_gateway` (hosted invoice; `x-nowpayments-sig` HMAC-SHA512 over key-sorted JSON). Migration `0005_phase4_payments` adds `deposit` to `transaction_type` + `payments` + `payment_events`. Routes: `GET /wallet/me`, `POST /wallet/deposit` (**KYC-gated**, `Idempotency-Key` required), `GET /wallet/transactions`, `GET /payments/{id}`, public `POST /payments/webhooks/stripe` + `/nowpayments`. Honest **503** until each provider is configured. Withdrawals stay disabled (Phase 7).
  - Frontend: `walletApi`/`paymentApi` in `api.ts`; `InvestorWallet.tsx` wired — live balance + ledger from the API, the dead **Deposit** button now creates a deposit → redirects to hosted checkout → credit lands via webhook; Withdraw disabled; Bank-transfer + Pronova shown `NOT_YET_ENABLED`.
  - Tests: **+19** — `test_payments_gateway.py` (Stripe/NOWPayments signature verify + status mapping), `test_wallet_deposit_db.py` (KYC gate, Idempotency-Key required + replay, 503, webhook credits once, replay no-op, captured-amount authority, forged-sig 401, failed-no-credit, NOWPayments credit, reconciliation), `test_wallet_routes_wired.py`.

**Phase 4 verified live:** owner tested a Stripe deposit end-to-end locally (via Stripe CLI `stripe listen`) — wallet credited correctly, ledger balanced (a $5000 deposit shows in Transactions + Wallets). NOWPayments rail is BUILT but UNTESTED locally (IPN needs a public URL; proven only via simulated signed IPN in tests). Admin panel polished: every user-keyed table (Users, KYC, User Roles, Wallets, Transactions, Investments) shows the **email** (view-only `user` relationship + `User.__str__`) and is searchable by email; Users & KYC have one-click **Verify/Reject KYC** actions; WTForms pinned `<3.2` (sqladmin edit-form compat).

- **Phase 5 — Investment Engine (Buy Units)** ✅ SECOND MONEY PHASE (design signed off; fee model corrected before build)
  - **Final fee model (owner-corrected):** **platform fee** (default 2.5%) charged to the buyer **AT PURCHASE**; **management fee** (default 1.0%) is **annual, deducted from distributions in Phase 6** — disclosed at purchase, **no money moves now** (matches the existing UI note, so the frontend stays truthful unchanged). **Both rates are admin-configurable from `platform_settings`** (not constants); the frontend's hardcoded 3%/1% are replaced by backend-supplied values.
  - **Two rails, server-authoritative:** **wallet-funded** = fully atomic in ONE tx (lock property `FOR UPDATE` → lock wallet → debit subtotal+fee → allocate units → ownership ledger → flip `funded` on last unit; lock order **property → wallet**). **Direct-pay** (Stripe card / NOWPayments crypto) = reserve units immediately (decrement `available_units` under the property lock, `pending` investment with a **30-min `reservation_expires_at`**) → signed webhook confirms (reuses Phase 4 verify+dedupe) → on confirm, book money + ownership ledger. Client never sends price/fee.
  - **Oversell protection:** `SELECT … FOR UPDATE` on the property row serializes buyers; DB **CHECK `available_units >= 0`** is the backstop. **Reservation lifecycle:** `expire_reservations` sweep (cron-able admin endpoint `POST /api/v1/investments/maintenance/expire-reservations`, `SKIP LOCKED`) releases lapsed unpaid holds → `expired`; a **late webhook after expiry reconciles** (re-acquire units if still free → confirm; else **refund the captured amount to the buyer's wallet**). Idempotency via `Idempotency-Key` (unique on `investments`); fully-funded flip inside the locked tx; losing race → `409`.
  - Backend: migration `0006_phase5_investments` (`platform_settings` seeded 2.5/1.0; append-only `ownership_ledger`; `investments` gains `idempotency_key`(uniq)/`reservation_expires_at`/fee+price snapshots/`payment_id`/`confirmed_via`/`failure_reason`; `investment_status` gains `expired`; CHECK `available_units>=0`). New: `settings_service` (reads fee rates), `investment_service` (create/confirm/reconcile/expire), `wallet_service.debit` (itemized signed ledger rows). `payment_service.process_webhook` branches on `purpose` (deposit→credit wallet; investment→confirm/release). Routes `POST/GET /api/v1/investments`, `GET /{id}`, admin expire-reservations. Admin panel gains **editable `platform_settings`** (owner changes fee rates, no deploy) + read-only `ownership_ledger`.
  - Frontend (no redesign — data/handler rewires only): `investApi`; `InvestmentCalculator` (ready properties) reads **fee rates from the backend** (`property.fees`, overlaid from `platform_settings`), adds a **"Wallet Balance"** method, **disables Pronova** (NOT_YET_ENABLED, no discount — D5), wires the CTA to `investApi` with `Idempotency-Key` (wallet→toast+confirmed; card→redirect to hosted checkout). **Installment plans (under-construction `InstallmentCalculator`) stay a stub — deferred to their own phase** (recurring scheduled payments are a bigger build).
  - Tests: **+19** — `test_investment_db.py`: KYC gate, Idempotency-Key required + replay, AMOUNT_TOO_LOW (sub-unit + below-minimum), PROPERTY_NOT_OPEN, **wallet happy-path** (units/fee/total + balance + ledger items + ownership + invariants), **atomicity rollback** on insufficient funds, **oversell 409**, **concurrent double-buy-of-last-units** (exactly one wins, no oversell), **DB CHECK backstop**, **direct-pay reserve→webhook-confirm** (+ idempotent replay), **failed-webhook releases reservation**, **reservation-expiry sweep**, **late-webhook reconcile** (re-acquire) + **refund-to-wallet when sold out**, direct-pay 503 unconfigured, list/get, auth. Invariants asserted: `confirmed+reserved+available == total`, `balance == SUM(ledger)`.

**Test status:** backend ruff + black + mypy + pytest (**96 tests**, incl. 50 DB-backed/admin/payment/investment) green; frontend `tsc`, eslint (**0 errors**, 11 pre-existing warnings), `vitest` (**8** — +2 invest click-path), `vite build` green. (Also fixed a pre-existing `eslint` error in `tailwind.config.ts` — `require()` → `import`.)

**Phase 5 post-test fix (dead Invest button):** owner caught in-browser that the Invest button did nothing — root cause was **routing, not the engine**: all 7 seeded properties have `demo-*` slugs, so the marketplace routed every card to `SamplePropertyDetails` (`/property-sample/:slug`) or `AdvancedPropertyPage` (`/advanced-property/:model`), **both of which had unwired "Invest" buttons (no onClick)**. The wired `InvestmentCalculator` only lived on `PropertyDetails` (`/property/:id`), which the seeded data never reached. Fix: both info-page CTAs now funnel to `/property/{slug|id}` (PropertyDetails resolves slug or id) → the single wired+tested calculator. Added a **component-level vitest** (`InvestmentCalculator.test.tsx`) that drives Invest → Confirm & Pay and asserts `investApi.create` is called with `{property_id, amount, method:'wallet'}` + an Idempotency-Key (a dead button now fails CI), plus a `ResizeObserver` polyfill in the test setup for Radix. Wallet-success now also invalidates the property/wallet/portfolio queries so the UI refreshes.

---

- **Phase 6 — Returns & Distributions** ✅ THIRD MONEY PHASE (design signed off; §8 decisions a/b/c/d all taken as recommended)
  - **Pro-rata = Hamilton largest-remainder in integer cents** (`distribution_service.hamilton`): floor each ideal share, hand leftover cents to the largest remainders (tie-break weight desc → key asc). **Σ payouts == gross_pool exactly**, deterministic, all `Decimal`/int (no float). Used for both the investor split and the family sub-split.
  - **Management fee (rental only):** withheld from each gross share at the **per-investment snapshot rate** (`investments.management_fee_rate`, frozen at purchase) × `period_fraction = days/365`, capped at the gross share. **Retained as recorded revenue** (no platform wallet in v1 — decision a); net credited to the wallet. `Σ net + Σ fee == gross_pool`. Appreciation/other kinds carry **no** fee (decision d).
  - **Idempotency (two layers):** `distributions` UNIQUE(property_id, period_key) → re-running a period is refused **409 DISTRIBUTION_EXISTS** (no double-pay); `distribution_items` UNIQUE(distribution_id, user_id). Period model = free-form `period_key` + explicit dates (decision c).
  - **Atomic:** one tx — `SELECT … FOR UPDATE` on the property (freezes ownership), wallets locked in **sorted user_id** order (lock order property→wallet), credit net (`transactions(type='return')` + `audit_log`) + `wallets.total_returns +=`, write `distribution_items`, set totals + `completed`. Any failure → full rollback (no partial credits). Ownership read from the append-only `ownership_ledger`.
  - **Family (record-only, decision b):** a group owner's net is split among members by `allocated_units` (Hamilton, exact) into `family_return_allocations` (+`member.allocated_returns`, `group.total_returns`); the **money stays in the owner's wallet**.
  - Backend: migration `0007_phase6_distributions` (`distributions` + `distribution_items` + `family_return_allocations.distribution_id`; no enum change — `transaction_type` already has `return`/`fee`). `distribution_service` (run + Hamilton + `my_returns`). Routes: admin `POST /api/v1/admin/properties/{id}/distributions` (AdminDep, idempotent), investor `GET /api/v1/investments/returns`. **SQLAdmin "Distributions" create-form triggers a real run** (`insert_model` → service; clean 409/422 surfaced as form errors) + read-only `DistributionItem` history.
  - Frontend: `returnsApi.getMine()`; **`ReturnsTracker` fully de-mocked** — retired the hardcoded `monthlyReturns`/`cumulativeReturns`/`dividendHistory`/`returnsSummary`/`totalReturns=19000`; charts + KPIs + a "Distribution History" table now driven by live `distribution_items`, with honest empty states. The fabricated **"5% reinvest discount" badge/copy removed** (D5 — reinvest = invest from wallet balance, no discount).
  - Tests: **+11** — `test_distribution_db.py`: pure Hamilton exactness+determinism on prime/uneven pools; 3-way pro-rata sums to pool to the cent; management-fee math (`Σ net + Σ fee == pool`, snapshot rate × period); appreciation = no fee; **re-run → 409, no double-pay**; **atomicity rollback** (inject 2nd-credit failure → no distribution/items/credits); **family split sums exactly to net**, money in owner wallet; admin gating (401/403); NO_OWNERS 422; `/returns` endpoint. Reconciliation invariants asserted (`balance == SUM(ledger)`, `total_returns`).

**Test status:** backend ruff + black + mypy + pytest (**107 tests**, incl. 61 DB-backed/admin/payment/investment/distribution) green; frontend `tsc`, eslint (**0 errors**, 11 pre-existing warnings), `vitest` (8), `vite build` green. Running DB migrated to **0007**; backend restarted (Phase 6 routes live).

---

- **Phase 7 — Withdrawals & Payouts** ✅ FOURTH MONEY PHASE — first time money LEAVES the platform (design signed off; D3 resolved: both rails, $5k auto-limit, no daily cap, KYC-gated)
  - **Hold-on-request (money-out safety):** a withdrawal request atomically debits spendable `balance` and mirrors the in-flight amount in `wallets.pending_balance`, under `SELECT … FOR UPDATE` on the wallet. The debit is booked immediately as a `transactions(type='withdrawal', -amount, status='pending')` row, so **`balance == SUM(ledger)` holds at once**; `pending_balance == Σ non-terminal withdrawals`. The provider is **never called inside the request tx**.
  - **Three double-pay guards:** `withdrawals.idempotency_key` UNIQUE (request replay → same withdrawal, one hold); **provider idempotency key = `withdrawal.id`** (a retried submit can't send twice); **status-transition guard under FOR UPDATE** (executor acts only on `approved`→`processing`; webhook settles only `processing`→`completed`). Webhook dedupe via `payout_events` UNIQUE(provider, event_id).
  - **States:** `pending_review` (> limit) → admin approve/reject; `approved` → executor submits → `processing` → webhook → `completed`; `failed`/`returned`/`rejected` → **idempotent compensating credit** (funds returned, never lost). **Reconciliation sweep** re-queries the provider for stuck `processing` rows (cron-able).
  - **Threshold:** `withdrawal_auto_approve_limit` (platform_settings, default **$5000**, admin-editable; also in `settings_service.DEFAULTS`). ≤ limit auto-approves; above → review queue.
  - **Rails:** **bank = Stripe Connect** (investor onboards as a connected account; bank payout = Transfer→connected account; bank withdrawals gated on a payouts-enabled account). **crypto = NOWPayments payout** (JWT, to a user-supplied address; works independently of Connect). Tokenized destinations only (Connect account id / crypto address) — never raw bank PII. Honest **503** per rail when unconfigured.
  - Backend: migration `0008_phase7_withdrawals` (`withdrawals`, `payout_events`, `connect_accounts`; seed `withdrawal_auto_approve_limit=5000`; transactions/wallets sufficed). New: `withdrawal_service` (request/execute/review/webhook/reconcile), `connect_service` (onboarding+status), `wallet_service.hold_for_withdrawal`/`settle_withdrawal`/`release_hold`. Gateways gained Stripe Connect (create account/link/status, transfer payout, payout+account webhook parse, get_payout_status) + NOWPayments payout (JWT, IPN verify, get_payout_status). Routes: `POST/GET /wallet/withdrawals`, `POST /wallet/connect/onboard`, `GET /wallet/connect/status`, admin `approve`/`reject`/`execute`/`reconcile`, payout webhooks `/payments/webhooks/stripe-payouts` + `/nowpayments-payouts`. SQLAdmin `WithdrawalAdmin` (Approve/Reject actions) + read-only `ConnectAccountAdmin`. Config: `nowpayments_email`/`password` (+ `nowpayments_payout_configured`, `stripe_connect_configured`).
  - Frontend: `withdrawApi` + `connectApi`; **InvestorWallet Withdraw dialog enabled** (amount + method Bank/Crypto + crypto address; bank shows **"Link your bank"** Stripe Connect onboarding until payouts-enabled; Idempotency-Key; success invalidates wallet/ledger; honest per-rail messages). LP-dashboard withdraw buttons made honest (point to Phase 9 LP economics — see deferred debt).
  - Tests: **+17** — `test_withdrawal_db.py`: over-balance 422; exact-balance OK + hold; **concurrent double-withdraw can't double-spend**; request idempotency (one hold); atomicity rollback; auto-approve ≤ limit vs review > limit; **rejected review releases funds**; execute→webhook completes (+replay deduped); **failed payout returns funds**; executor submit-failure releases; **bank Stripe completes then returned → funds back**; reconcile settles stuck `processing`; admin gating; 503 per rail; CONNECT_NOT_READY. Invariants asserted (`balance == SUM(ledger)` incl. holds; `pending_balance == Σ non-terminal`).

**Test status:** backend ruff + black + mypy + pytest (**124 tests**, incl. 78 DB-backed/money) green; frontend `tsc`, eslint (**0 errors**, 11 pre-existing warnings), `vitest` (8), `vite build` green. Running DB migrated to **0008**; backend restarted (Phase 7 routes live).

---

- **Phase 8 — Secondary Market (Investor-to-Investor Resale)** ✅ FIFTH MONEY PHASE (design signed off; D8 decisions all built as approved)
  - **Atomic transfer:** buying off a listing is ONE tx — debit buyer (gross **+ resale fee**), credit seller the **FULL gross**, move units via two `ownership_ledger` rows (seller −M `secondary_sale` / buyer +M `secondary_purchase` — **Σ per property conserved**), resale fee **retained as recorded revenue** (no platform wallet, same as Phase 6 mgmt fee), `transactions` (`secondary_sale` for seller; `investment`+`fee` for buyer) + `audit_log`. Any failure → full rollback (insufficient balance leaves the listing + ledger untouched).
  - **Concurrency / oversell:** lock order **listing → property → wallets** (wallets locked in sorted `user_id` order). `SELECT … FOR UPDATE` on the **listing row** serializes buyers (exactly one wins; racers get `409`); partial fills decrement `units_remaining` (DB **CHECK ≥ 0** backstop); last fill flips the listing to `sold`. Buyer **KYC-gated** + **`Idempotency-Key`** (UNIQUE on `secondary_trades` → replay returns the same trade, never double-buys).
  - **Listing rules:** ownership validated from `ownership_ledger` (net held); the active listing **reserves** units (`holding − Σ active-listing units_remaining`, under the property lock) so you can't double-list; **lock-up** (ref = seller's earliest acquisition, default 0 → `409 LOCKUP_ACTIVE`) + **price bounds** (ref = property `unit_price`, default open → `422 PRICE_OUT_OF_BOUNDS`); **resale fee buyer-side on top** (default **1.0%**). All four knobs admin-configurable in `platform_settings` (`secondary_resale_fee_pct`/`secondary_lockup_days`/`secondary_price_min_pct`/`secondary_price_max_pct`), mirrored in `settings_service.DEFAULTS`.
  - **Ownership integrity:** `ownership_ledger` stays the source of truth; a trade nets zero on Σ-units, so primary invariants (`confirmed + reserved + available == total`) are unaffected; **returns follow the new owner** (distribution reads ownership from the ledger — test-asserted).
  - Backend: migration `0009_phase8_secondary` (upgrade `secondary_listings`: add `property_id` FK + `units_remaining` + `cancelled_at`, `investment_id` → nullable, CHECK ≥ 0, statuses active|sold|cancelled; new `secondary_trades` w/ UNIQUE idempotency_key; `transaction_type` += `secondary_sale`; settings seeds). New `secondary_service` (create/cancel/buy/holdings + reads), `schemas/secondary.py`, `routes/secondary.py` (`POST/GET /secondary/listings`, `/mine`, `/{id}/cancel`, `/{id}/buy`, `GET /holdings`, `GET /settings`). SQLAdmin gains read-only **Secondary Listing** + **Secondary Trade** views.
  - Frontend (data/handler rewires, no redesign): `secondaryApi` + `holdingsApi`; **`SecondaryMarket` Buy tab DB-backed**, the dead **"Confirm Purchase"** button wired → `secondaryApi.buy` with `Idempotency-Key` + **backend-driven resale-fee display** + KYC-error toast; **"My Listings"** tab (live, with cancel); `SellUnitsForm` DB-backed (holdings dropdown, real listing creation, honest fee copy — seller gets full gross). **`exitStore` localStorage mock RETIRED** — now an API-backed module (`useExitRequests`→ my listings, `useOwnedPositions`→ holdings, `useCancelExitRequest`, `useCreateListing`); `ExitFlowDialog` secondary path creates a real listing, **liquidity path honest → Phase 9** (D7); `ExitRequestsPanel` live.
  - Tests: **+15** — `test_secondary_db.py`: list-units-you-don't-own → 422; double-listing reservation → 422; lock-up → 409 (and clears past window); price bounds → 422 (low/high) + ok; happy path (units + money move, fee correct, partial fill); full-fill → `sold`; can't-buy-own → 409; insufficient balance → 422 rollback; buyer idempotency replay; Idempotency-Key required; KYC gate; **concurrent double-buy → one wins / one 409, units_remaining never negative**; **ownership invariants after a randomized burst** (Σ conserved, balance == SUM(ledger) per participant); **sell-then-distribution pays the NEW owner**; auth.

**Test status:** backend ruff + black + mypy + pytest (**139 tests**, incl. 93 DB-backed/money) green; frontend `tsc`, eslint (0 errors), `vitest` (**9** — +1 secondary buy click-path), `vite build` green. Running DB migrated to **0009**. ⚠️ **Restart the backend** (new `secondary` router) before re-testing in the browser.

**Deferred Phase-8 nuance (logged):** a secondary BUYER gets `ownership_ledger` units but **no `investments` row**, so the Phase-6 management fee (rental, computed from `investments.management_fee_rate`) is **not** withheld from a resold buyer's rental distributions in v1. Pro-rata gross is still correct (ownership-driven). Revisit if mgmt-fee-on-resold-units is required (would attach a fee-rate snapshot to `secondary_trades` and have the distribution fee base read ownership, not investments).

---

- **Phase 9 — Liquidity Provider Module: ACTIVE rail + schema** ✅ FIFTH+ MONEY PHASE (D7 resolved; design + reviewer fixes signed off; PASSIVE engine deferred to next step by build order)
  - **Two products, never commingled.** ACTIVE = LP funds a seller's instant-exit at a liquidity discount (risk-bearing, variable). PASSIVE = fixed-APY locked pool (schema only this build; engine hard-locked OFF). The **never-commingle invariant** is enforced in `lp_positions`: single-valued `classification` CHECK + two cross-field CHECKs forcing the opposite product's columns NULL (one row = one product), one atomic debit → one classified row, ACTIVE↔`ownership_ledger` / PASSIVE↔no-units. (DB-tested: malformed active/passive rows rejected by the CHECK.)
  - **Atomic ACTIVE buyback** (mirrors Phase 8): lock order **exit_request → property → wallets (sorted `str(user_id)`)**; LP debited `lp_price` (`investment`), seller credited `lp_price` (`secondary_sale`) with the liquidity fee withheld as an explicit `fee` row (retained revenue) → seller nets `seller_net`; units move seller→LP in `ownership_ledger` (Σ/property conserved); `FOR UPDATE` on the request row → one LP wins / racers 409; `units_remaining >= 0` CHECK backstop; KYC + `liquidity_provider` role + `Idempotency-Key`. Partial fills supported.
  - **Seller price-lock:** the request's pricing snapshot (`unit_price/discount/fee/lp_price/liquidity_fee/seller_net`) is **authoritative** — the seller is paid the agreed `seller_net` verbatim; the fill only **band-checks** (`lp_exit_price_band_pct`, default 10%) and rejects a stale fill (409 `PRICE_BAND_EXCEEDED`), never silently re-prices. Server-authoritative discount/fee from `platform_settings` off `unit_price`.
  - **Cross-market reservation (symmetric):** shared `secondary_service.reserved_units` now counts active secondary listings **+** open LP exit requests; **patched Phase-8 `create_listing`** to honor it. A unit can't sit on both markets in either order (tested both directions).
  - **Decision 1 (Returns labeling):** LP "Realized Cash Flows" tab shows rental distributions + resale-proceeds note; **no per-deal P&L / no "profit"/"returns"/"APY" for ACTIVE** (no per-lot cost basis); positions shown as acquisition audit; Backed Assets read live from `ownership_ledger`.
  - **Decision 2 (mgmt-fee leak CLOSED, shipped with ACTIVE):** `ownership_ledger.fee_rate` stamped at acquisition (originals keep their agreed rate; LP/secondary carry the platform rate at acquisition; Phase-5 rows backfilled). Phase-6 fee base rewritten to ownership-derived `Σ(units×unit_price×fee_rate) × held_ratio` — **never a global re-derive**, so no investor's agreed fee changes retroactively. (Tested: LP-acquired units now charged mgmt fee; original investor charged their own rate.)
  - **Decision 3 (PASSIVE hard lock):** `lp_passive_enabled` setting default **false**; tiers in `lp_pool_tiers` (8/10/12/15% indicative). PASSIVE engine NOT built this step; the dashboard "Fixed-Yield Pool" tab is honestly disabled ("not yet open", rates labeled **indicative targets, not guaranteed**). **"Regulated by FSA" still NOT rendered** anywhere.
  - Backend: migration `0010_phase9_liquidity` (`lp_pool_tiers` + seed, `lp_exit_requests`, `lp_positions` w/ CHECKs, `ownership_ledger.fee_rate` + backfill, `transaction_type` += `lp_deposit`/`lp_yield`, 5 platform_settings seeds). New `liquidity_service` (ACTIVE), `schemas/liquidity.py`, `routes/liquidity.py`. SQLAdmin: read-only LP Exit Request / LP Position + editable LP Pool Tier.
  - Frontend (ACTIVE end-to-end): `liquidityApi`; **`LiquidityProviderMarket`** DB-backed order book + wired **"Confirm Allocation"** (`liquidityApi.fund` + Idempotency-Key + KYC/role-error toasts); **`ExitFlowDialog`** liquidity path now creates a **real exit request** (no more Phase-9 toast); **`LiquidityDashboard`** rebuilt to Decision-1 labeling + live Backed Assets + honest disabled PASSIVE tab.
  - Tests: **+18** — `test_liquidity_db.py`: pricing snapshot; not-owner; cross-market reservation (both directions); atomic happy path (fee residual + ownership conservation + invariants); seller price-lock in-band; price-band reject; can't-fund-own; insufficient-balance rollback; idempotency; KYC+role gate; partial fill; expiry; **Decision-2 mgmt-fee charged on LP units**; **never-commingle CHECK**; auth. Plus frontend `LiquidityProviderMarket.test.tsx` (fund click-path guard).

**Test status:** backend ruff + black + mypy + pytest (**157 tests**) green; frontend `tsc`, eslint (0 errors, 1 fixed), `vitest` (**10** — +1 LP fund), `vite build` green. Running DB migrated to **0010**. ⚠️ **Restart the backend** (new `liquidity` router) before browser re-testing.

---

## Phase 9 ACTIVE — VERIFIED ✅ (owner tested the buyback end-to-end 2026-06-23)

Owner confirmed: buyback moved wallets + units correctly; **Decision-2 mgmt-fee leak fix verified** (LP charged the fee on acquired units; original investor's rate unchanged). Demo seed: `backend/scripts/seed_phase9_demo.py` (idempotent) — SELLER `seller.phase9@capimax.com` / LP `lp.phase9@capimax.com` / `Passw0rd!23`, property "Sample - Phase 9 Ready Demo".

## ⛔ PASSIVE engine — DEFERRED (owner decision 2026-06-23), NOT forgotten

**Do NOT build the PASSIVE rail now.** It stays **hard-locked** (`lp_passive_enabled=false`); the schema (`lp_pool_tiers`, `lp_positions` passive columns) is in place but no deposit/accrual/maturity/redeem engine is built and **no real deposit can be taken**. PASSIVE is **blocked on a business input the owner owes**, tracked as an open decision:
- **(a) yield-funding source + reserve buffer** — where the guaranteed fixed APY actually comes from.
- **(b) written ALM rules** — how maturity redemptions are met from liquidity; what happens when deployment yield < promised APY.
- **(c) capital/reserve adequacy** — ability to honor the guarantee.
- **(d) FSA license confirmation** with regulatory counsel — **no live PASSIVE offering and the "Regulated by FSA" claim stays unrendered** until confirmed.
Until all four exist, fixed APY must never render as "guaranteed". (See also Still-open decisions table.)

- **Phase 10 — Family Groups & Gifting (real on-ledger co-investment)** ✅ SIXTH MONEY PHASE (design + reviewer foci signed off; discount mechanic = discounted effective unit price)
  - **REAL vs PENDING (disjoint) model:** a family unit is either REAL (in a KYC'd member's `ownership_ledger`, paid by Phase-6 directly) or PENDING (still in the from-holder's ledger, reserved for a not-yet-registered member, recorded as a `pending` `family_transfers` row). Materializes to a real ledger move on the member's KYC-verify. A unit is never both — the basis for the invariant + no-double-count.
  - **§2 ≤-ownership invariant:** the shared `secondary_service.reserved_units(user, property)` helper now also counts **pending family-transfers-out** (alongside secondary listings + LP exit requests); every allocate/transfer is checked `units ≤ holding − reserved` under the property `FOR UPDATE`. A unit can never be simultaneously listed (P8), exit-requested (P9), or family-allocated — tested both directions.
  - **§3 no-double-count:** `_allocate_family` rewritten — of the holder's net for a property, only the **pending fraction** (`holder_net × pending_total/holder_units`) is recorded to pending members; the rest stays the holder's. REAL-owner members are paid directly and **excluded** from the split. `Σ direct items + record-only == pool`, no unit twice (test asserts owner 1200 + B 800 == 2000, pending P recorded 400).
  - **Atomic transfer (reuses P8):** REAL = ledger −N/+N under property lock, Σ conserved, **fee_rate stamped (Decision-2 carries)**, zero price/no wallet legs; PENDING = reserve + record; Idempotency-Key UNIQUE; concurrency serializes on the property lock.
  - **Materialization:** on member KYC-verify (auto-hook in `kyc_service` + self-serve `POST /family/materialize`), pending transfers convert to real atomic moves, `allocated_units` zeroed, accrued record-only returns swept to the member's wallet. Idempotent.
  - **`allocated_units` setter:** the transfer flow is now the writer (closed the Phase-6 orphaned-column gap); redefined as the **pending-units cache**, real units always read from `ownership_ledger`.
  - **Discount (§7):** one admin setting `family_reinvest_discount_pct` (default **7.5**, family-scoped only — D5 holds elsewhere; "Pronova 2.5%" is just a label, Pronova system stays disabled) + `family_transfer_fee_pct` (default 0). Reinvest buys units at `effective_price = unit_price × (1 − d/100)`, `units = floor(amount/effective_price)`, server-authoritative.
  - Backend: migration `0011_phase10_family` (`family_transfers` += property_id/idempotency_key/materialized_at; `family_groups` UNIQUE(owner_id); `transaction_type` += `family_allocation`; 2 settings seeds). New `family_service` (group/member/transfer/allocate/reinvest/materialize), `schemas/family.py`, `routes/family.py`. SQLAdmin: read-only Family Group/Member/Transfer.
  - Frontend: `familyApi`; **`FamilyInvestment.tsx` Members/Transfers/Allocations tabs wired to real APIs**, mock arrays retired ("Ahmed Al-Hassan"/`mockTransfers` gone); create-group CTA; property selector required on transfers; real-vs-pending shown via the existing badges; reinvest dialog at the family discount. **Estate/Gifting tabs:** mock UI present; real backend deferred to the final group (CapiMax's own scope — see the "New direction" correction above; the old "BRX / out of scope" note was wrong).
  - Tests: **+15** — `test_family_db.py`: one-group-per-owner; add real vs pending member; over-allocation → 422; pending reserves block a secondary listing **and** LP exit (both directions); atomic real transfer (ledger conserved, fee_rate stamped, no wallet movement) vs pending (reserve only); materialize on KYC; **no-double-count** (Σ items == pool, pending recorded-only); discount yields `floor(amount/(unit_price×(1−d)))` + configurable; idempotency (transfer + reinvest); KYC/auth gates. Existing Phase-6 family test updated to the pending-transfer model. Plus frontend `FamilyInvestment.test.tsx`.

**Test status:** backend ruff + black + mypy + pytest (**172 tests**) green; frontend `tsc`, eslint (0 errors), `vitest` (**12** — +2 family), `vite build` green. Running DB migrated to **0011**. ⚠️ **Restart the backend** (new `family` router) before browser re-testing.

- **Phase 11 — Broker Referrals & Commissions** ✅ SEVENTH MONEY PHASE (credits broker wallets; design + two read-backs + no-retroactive-linking signed off)
  - **Revenue-based commission, structurally capped.** A broker earns `broker_commission_pct` × the **platform revenue attributable to a referred client** — NEVER a percent of the investment amount. v1 revenue events: the **Phase-5 purchase platform fee** (one-time at confirmation) + the **Phase-6 rental management fee** (recurring while holding). Secondary-resale (P8) and liquidity (P9) fees are **deliberately OUT** (paying on a client's exit is a perverse incentive). A DB **CHECK `commission_amount <= revenue_amount`** makes overpaying the broker structurally impossible.
  - **Signup-only broker→client link.** `broker_service.resolve_signup_referral` is the ONLY writer of `broker_referrals` (called from `auth_service.register`). A broker code creates the immutable link (`UNIQUE(client_id)`, `CHECK broker_id<>client_id`) + sets `users.referred_by`; anything else falls back to raw user→user attribution that **earns no commission** (anti-self-referral-fraud). **No retroactive linking** — a client who signed up without a code can never be adopted later.
  - **Accrual engine (`broker_service.accrue_commission`).** Single reusable hook: looks up the referral (none → no-op), computes `rate × revenue`, credits the broker wallet (`referral_commission` txn), writes an append-only `broker_commissions` row. **Idempotent** — `UNIQUE(revenue_event_type, revenue_event_id)` + SELECT-first guard ⇒ one revenue event → ≤1 accrual. **Rate snapshot per row** ⇒ an admin rate change never rewrites history. **No clawback** — append-only; full exit → 0 units → 0 mgmt fee → 0 commission (structural, no special code).
  - **Atomic + global lock order.** Hook A (Phase-6 distribution): pre-locks the **union of owner wallets + their referring brokers** in the sorted `str(user_id)` order (new `wallet_service.lock_wallets`) before the credit loop, then accrues per `distribution_item`. Hook B (Phase-5 confirm — wallet + webhook + late-reconcile paths): locks `sorted([buyer, broker])`, accrues on `investment.platform_fee_amount`. No deadlock across concurrent shared-broker runs (tested).
  - **Rate from settings, zero literals.** `broker_commission_pct` (default **10.0**) in `platform_settings` + `settings_service.DEFAULTS` + `get_broker_commission_pct()`; flows to the accrual calc and every display surface; no inline rate anywhere.
  - Backend: migration `0012_phase11_broker` (`broker_codes`, `broker_referrals`, `broker_commissions` w/ UNIQUE + 2 CHECKs; seed `broker_commission_pct`; no enum change — `referral_commission` already in 0001). New `broker_service`, `schemas/broker.py`, `routes/broker.py` (broker-role-gated: `/referral-code`, `/dashboard`, `/referrals`, `/commissions`). Hooks wired into `investment_service` + `distribution_service`; link resolution in `auth_service.register`. SQLAdmin: read-only Broker Code / Referral / Commission.
  - Frontend: `brokerApi`; **`BrokerDashboard` de-mocked** (retired `brokerStats`/`referrals`/`commissionData` + "Premium Realty Partners"/"Ahmed Al-Farsi"/"$125,000"): live stats, a **copyable referral link**, live referrals + commission ledger, monthly chart aggregated from real commissions, deep-linkable tabs (`?tab=`). **Wallet tab reuses the live `InvestorWallet`** (commissions land in the same wallet; Phase-7 withdraw). **`Auth` captures `?ref=CODE`** (+ editable field) → `referral_code` at register. **Virtual Cards honest-disabled across ALL roles** (`VirtualCardRequest` → "not yet available", request button disabled, no fake issuance — D9). **Dead broker sidebar routes resolved:** Referrals/Commissions/Wallet repoint to real dashboard tabs; **Reports & Documents removed** (out of v1); Exit Mechanisms / Property Types / Secondary / Liquidity kept (already real).
  - Tests: **+16** — `test_broker_db.py`: commission = pct × actual platform revenue (not investment amount); no accrual at signup; accrual per event (purchase + each rental); stops on full exit; no clawback; idempotent (distribution re-run 409 + same-event no-op); admin rate change reflected + history immutable; self-referral / generic path no commission; structural cap CHECK; atomicity rollback (injected accrual failure) + `balance == SUM(ledger)`; **no deadlock under concurrent shared-broker distributions**; role gating/auth; referral code stable/unique/one-per-broker; client link immutable. Plus frontend `BrokerDashboard.test.tsx` (live data, mock retired, cards honest-disabled).

**Test status:** backend ruff + black + mypy + pytest (**188 tests**) green; frontend `tsc`, eslint (0 errors, 11 pre-existing warnings), `vitest` (**14** — +2 broker), `vite build` green. Running DB migrated to **0012**. ⚠️ **Restart the backend** (new `broker` router) before browser re-testing.

- **Phase 12 — Notifications + Email** ✅ (in-app read path + transactional email outbox; documents deferred)
  - **In-app read path (the core deliverable):** `notify()` was write-only across 11 points; added `GET /notifications` (+ `unread_count`), `GET /notifications/unread-count`, `POST /{id}/read`, `POST /read-all` (own rows only). The bell + a real `/notifications` page consume it.
  - **Transactional email outbox (owner-approved):** an email-eligible event writes the in-app row **and** an `email_outbox` row **atomically inside the originating (often money) tx**; a separate cron drainer (`email_service.dispatch_pending`, `SKIP LOCKED`, retry, idempotent) sends OUT of any money tx. **Email is never sent inside a financial transaction** — a slow/failing Resend can't stall or roll back a distribution/withdrawal. Console locally; **Resend live-verified on the VPS** (drainer is a Phase-13 cron).
  - **Email matrix (impossible-by-omission):** `notify()` extended backward-compatibly with `email_category`/`email_to`/`force_email`. **Email + in-app:** KYC decision, investment confirmed, investment refunded, return distributed, withdrawal settled, **broker commission** (was silent — notify added), **family invite** (real email to the invitee — `force_email`, `email_to`, non-user → `email_outbox.user_id` NULL). **In-app only:** KYC started, deposit credited, secondary sale, LP fill, family member-added, KYC under-review. An event without an `email_category` can never email.
  - **Preferences:** `notification_preferences` (per-user EMAIL toggles; in-app always on). `GET/PUT /notifications/preferences` — only the 4 email keys accepted; **SMS/push not persisted** (no delivery mechanism). The prefs gate email but **never** in-app.
  - **Channels = in-app + email ONLY.** AccountSettings SMS/push toggles honest-disabled ("Not available yet"); the 4 email toggles wired to real persistence (closes the logged "toggles don't persist" debt).
  - **Documents fully deferred** (needs app storage; D11 keeps generation out): `PropertyDocuments` + `InvestmentCertificates` mocks **honest-disabled, NOT deleted** (banner + disabled View/Download); the `/documents` "Documents Center" sidebar link removed. DELETE NOTHING held.
  - Backend: migration `0013_phase12_notifications` (notifications index, `notification_preferences`, `email_outbox`; no enum change). New `email_service` (drainer), extended `notification_service` (dispatch + read/prefs), `schemas/notifications.py`, `routes/notifications.py` (+ admin `POST /admin/notifications/dispatch-emails` cron). SQLAdmin: read-only Notification Preference + Email Outbox.
  - Frontend: `notificationApi`; **real bell** in `MainLayout` (hardcoded "3" killed → live unread count → `/notifications`); new `/notifications` page + route; AccountSettings email-prefs persistence + honest-disabled SMS/push; documents honest-disabled; `/documents` link removed.
  - Tests: **+16** — `test_notifications_db.py`: read path (own rows, unread-count, mark one/all, auth); **the email matrix** (email events write in-app + ONE outbox; in-app-only events write in-app + NO outbox); **prefs gate email never in-app**; **family invite → outbox for a NON-USER invitee** (force, user_id NULL); **broker commission now notifies**; **transactional atomicity** (rollback → no in-app AND no outbox; outbox stays 'pending' = send never in-tx); drainer (sent/retry/idempotent); preferences API (defaults + persist; sms/push keys ignored); admin-only dispatch gating. Plus frontend `Notifications.test.tsx` + `MainLayout.test.tsx` (live bell count, not "3").

**Test status:** backend ruff + black + mypy + pytest (**204 tests**) green; frontend `tsc`, eslint (0 errors, 11 pre-existing warnings), `vitest` (**18** — +4 notifications/bell), `vite build` green. Running DB migrated to **0013**. ⚠️ **Restart the backend** (new `notifications` router) before browser re-testing. **Cron to schedule on VPS:** the email drainer (`POST /api/v1/admin/notifications/dispatch-emails`). **Resend** is the live-verify item (console locally).

- **Phase 13 — Admin / Scheduled Jobs / Reconciliation / Hardening** ✅ (launch-prep; DELETE NOTHING throughout)
  - **Cron-auth seam:** `require_admin_or_cron` (deps) — admin JWT **OR** a constant-time `X-Cron-Secret` (config `cron_secret`; unset never authorizes). Applied to the 6 idempotent cron targets: withdrawals **execute** + **reconcile**, **expire-reservations**, email **dispatch-emails**, the new **LP expire-requests**, and **reconciliation**. Human-only admin actions keep `AdminDep`. Crontab entries are VPS-gated.
  - **LP-expiry sweep (real bug fix):** `liquidity_service.expire_open_requests` (`FOR UPDATE SKIP LOCKED`) flips lapsed `open` exit requests → `expired` so `secondary_service.reserved_units` (counts `status='open'`) releases them — closing the cross-market units-reservation leak (a lapsed request reserved units forever). No money/ledger move; idempotent. Endpoint `POST /api/v1/admin/liquidity/expire-requests`.
  - **Reconciliation report (read-only, DB-wide):** `reconciliation_service.run` scans 6 invariants — wallet balance == Σ ledger; pending_balance == Σ non-terminal withdrawals; property `total == available + Σ pending-reservation + Σ issued`; ownership non-negativity; family Σ pending ≤ holder net; distribution `Σ(net+fee) == gross_pool`. `GET /api/v1/admin/reconciliation` → `{ok, checks[]}` + **`scripts/reconcile.py`** (cron-able; **exit 1 on drift**). Detector only — never repairs.
  - **Settings validation:** `settings_service.validate_setting` — pcts 0–100, price-bounds ≥0/empty, ints ≥0, booleans; **`lp_passive_enabled` hard-locked (cannot be set `true`)**. Wired into SQLAdmin `PlatformSettingAdmin.on_model_change` so a garbage value is rejected (no more silent fallback).
  - **Portfolio (server-authoritative):** `GET /api/v1/investments/portfolio` — invested / current_value (Σ units×unit_price from `ownership_ledger`) / total_returns / properties / units. Replaces the client-side mock.
  - **Rate-limiting:** slowapi (IP-keyed) on `login`/`register`/`password/forgot` (10/10/5 per min) + the 4 public webhooks (120/min) → **429** in the error envelope. Disabled in tests; VPS note: point at Redis if multi-worker.
  - **Health:** `/healthz` Redis is **optional** — 200 `ok` when the DB is up regardless of Redis (still reported); 503 only when the DB is down.
  - **Dashboard mocks (DELETE NOTHING):** wired → `PortfolioOverview` (live portfolio; **$125k retired**; allocation/goals honest-disabled — no backend), `ActiveInvestments` (live `holdingsApi`), `SecondaryMarketTab` (routes to the live `/secondary-market`). Honest-disabled → **`ProShareCards`** (D9 card-disable that was missed in Phase 11) + `InstallmentSchedule` (deferred). InvestorDashboard greeting uses the **real user name** (retired "Welcome back, Ahmed"); the dashboard's own bell/settings now route to `/notifications` / `/settings`.
  - **Public marketing claims (Footer FSA / Hero $50M AUM / $125k ticker): LEFT AS-IS** — owner asserts substantiation exists; recorded as his call/responsibility. No code touched them.
  - Backend: **no migration** (LP-expiry reuses the existing `lp_exit_requests.status`; new value `expired`). New: `reconciliation_service`, `email`/cron-auth wiring, `core/ratelimit.py`, `routes/admin/liquidity.py` + `routes/admin/reconciliation.py`, `scripts/reconcile.py`; config `cron_secret`; dep `slowapi`. SQLAdmin settings editor now validates on save.
  - Tests: **+13** — `test_phase13_db.py`: cron-auth (secret/admin/missing/wrong/unset); LP-expiry (lapsed→expired→units freed→re-listable, idempotent, valid untouched); reconciliation (clean→ok; injected wallet/property/family drift→flagged; never mutates); settings validation (bad pcts/limits rejected, **passive-true rejected**); portfolio (real ledger values + zero-state + auth); rate-limit 429; health-with-Redis-down→200. Plus frontend `PortfolioOverview.test.tsx` (+4: live portfolio mock-retired, ProShareCards/InstallmentSchedule disabled, SecondaryMarketTab routes). Existing `test_health.py` updated to the optional-Redis contract.

**Test status:** backend ruff + black + mypy + pytest (**217 tests**) green; frontend `tsc`, eslint (0 errors, 11 pre-existing warnings), `vitest` (**22** — +4), `vite build` green. **No new migration** (DB stays at 0013). ⚠️ **Restart the backend** (new admin routers + rate-limit + cron-auth) before browser re-testing. **VPS:** set `CRON_SECRET`, schedule the 6 cron jobs, optionally provision Redis (rate-limit store / hot-row locks).

- **Phase 14 — Integration & Launch** ✅ (final build pass; DELETE NOTHING throughout)
  - **Reinvest discount is now REAL (2nd narrow D5 exception).** Removed the fake client-side `REINVEST_DISCOUNT_RATE = 0.05` (the server ignored it = a lie). New admin-configurable `reinvest_discount_pct` (default 5.0, validated 0–100, in `settings_service.DEFAULTS`). Server-authoritative `investment_service.reinvest_from_wallet` mirrors the family reinvest: `effective_price = unit_price × (1 − d/100)`, `units = floor(amount/effective_price)`, debit wallet `amount`, ownership at nominal price + stamped `fee_rate`, atomic (property→wallet lock), idempotent on the Idempotency-Key. New `POST /investments/reinvest` (KYC-gated) + `GET /investments/reinvest-settings`. Both surfaces (`ReinvestReturns`, `InvestmentCalculator` reinvest branch) now show the real server rate + call `investApi.reinvest`; `availableReturns` reads the real portfolio (the hardcoded `19000` is gone). Standard invest stays no-discount.
  - **Owner Financials + Wallet tabs wired to the real wallet** (`InvestorWallet` reused) — the `$125k`/`$77k`/`$892k` mocks + the fake `toast.success` "withdrawal initiated" + mock payout/transaction tables are retired; owners use the real Phase-4/7 balance + deposit + withdraw. Owner Documents tab honest-disabled (deferred storage).
  - **Email-verified badge** reflects the real `user.email_verified` (was hardcoded "Verified").
  - **Honest-disabled (kept, no fake success):** AccountSettings 2FA + Active Sessions; owner View Details/Analytics; developer View Details/Update Progress/Investor Report; `InstallmentCalculator` commit CTA (deferred); Partners "Contact Us" → routes to `/support`.
  - **Public marketing claims** (Footer FSA / Hero $50M AUM / $125k ticker): **left as-is** — owner asserts substantiation (his call; on the checklist's final compliance step).
  - **`plan/DEPLOYMENT_CHECKLIST.md`** authored — the consolidated owner-facing runbook: §1 providers (Stripe deposits + Connect, NOWPayments deposit + payout, Sumsub, Resend, Google + Apple OAuth — each with exact env var + webhook URL + provider config), §2 infra (VPS/Postgres/gunicorn/nginx, domain/TLS, JWT_SECRET, COOKIE_SECURE, CRON_SECRET, CORS, `alembic upgrade head`→0014 + `seed_admin`), §3 frontend env, §4 the 6 cron entries with `X-Cron-Secret`, §5 the live-verification runbook + reconciliation→zero-drift.
  - Backend: migration `0014_phase14_reinvest_discount` (seeds `reinvest_discount_pct`). New `reinvest_from_wallet` + `_reinvest_already_done` (audit-based idempotency), `portfolio`/reinvest schemas, `/investments/reinvest` + `/reinvest-settings` routes, `get_reinvest_discount_pct` + validation spec.
  - Tests: **+5 backend** (`test_reinvest_db.py`: server-applied discount; admin-configurable rate + live read; idempotent replay; insufficient funds; KYC/key/auth gates) **+1 frontend** (`InvestmentCalculator.reinvest.test.tsx`: reinvest confirm calls `investApi.reinvest` with only {property_id, amount} — never a client price — not `create`). PortfolioOverview test already guards the $125k retirement.

**Test status:** backend ruff + black + mypy + pytest (**222 tests**) green; frontend `tsc`, eslint (0 errors, 11 pre-existing warnings), `vitest` (**23**), `vite build` green. Running DB migrated to **0014**. ⚠️ **Restart the backend** (new reinvest routes) before browser re-testing.

## Currently: ALL 0–14 BUILD COMPLETE — only owner-gated live deployment remains

Work through **`plan/DEPLOYMENT_CHECKLIST.md`** (provider keys, VPS, crons, live verification). Nothing in the app is mock/fake except the explicitly-deferred, honestly-disabled features (PASSIVE engine, installments, documents/file-storage incl. avatar+property-image upload, virtual cards) and the estate/gifting feature — **all now slated to get real backends phase-by-phase per the "New direction" above** (estate is CapiMax's own scope, built last).

**Next: build real backends for the honest-disabled items, group by group (see "New direction" above)** — starting with Phase 15 (Owner/Developer stats), then storage→certificates, then payment methods, then the estate/inheritance engine (last, legal-design step). **Estate correction:** `FamilyBeneficiaryGifting.tsx` is **CapiMax's own feature** (NOT BRX) — keep its UI, build its real backend in the final group.

**To test Phase 13 locally:** restart the backend (new admin routers + rate-limit). (1) **Reconciliation:** `backend\.venv\Scripts\python.exe scripts/reconcile.py` on the seeded DB → "OK (no drift)", exit 0 (or hit `GET /api/v1/admin/reconciliation` as admin). (2) **LP-expiry:** with a lapsed `open` exit request, `POST /api/v1/admin/liquidity/expire-requests` (admin or `X-Cron-Secret`) → it flips to `expired` and the seller's units are freed (re-listable). (3) **Portfolio:** the investor dashboard **Overview** shows the **real** invested/value/returns (the $125k mock is gone); `GET /api/v1/investments/portfolio`. (4) **Settings:** in `/admin → Platform Setting`, a bad value (e.g. `platform_fee_pct=150`, or `lp_passive_enabled=true`) is rejected. (5) **Rate-limit:** >10 rapid `/auth/login` → 429.

Phase files: `plan/phase-0X-*.md`, `plan/phase-11-broker.md`, `plan/phase-12-notifications.md`, `plan/phase-13-hardening.md`. Local run + admin login + provider setup: see `plan/RUN_LOCAL.md`.

---

## ⚠️ DEFERRED VPS VERIFICATION STACK (growing — none verified live; owner chose to keep building)

All built + unit-tested with **mocked providers / simulated signed webhooks**; NOT yet exercised against real services. Must be deployed + tested on the **Hostinger VPS** (EU) with real creds + a public URL:
- **Real crypto deposit** (NOWPayments IPN) — built, only simulated-IPN tested.
- **Real withdrawals** — Stripe Connect (bank: onboarding + Transfer payout + `account.updated`/`payout.*` webhooks) **and** NOWPayments crypto payout (JWT + 2FA + IP-whitelist + funded balance).
- **OAuth** (Google/Apple), **real Sumsub KYC**, **Resend email**.
- **Crons (Phase 13 — all built + idempotent; authenticate via `X-Cron-Secret`; set `CRON_SECRET` on the VPS, then schedule):**
  - withdrawal executor — `POST /api/v1/admin/withdrawals/execute` (~1–5 min)
  - withdrawal reconcile — `POST /api/v1/admin/withdrawals/reconcile` (~15 min)
  - reservation-expiry sweep — `POST /api/v1/investments/maintenance/expire-reservations` (~5 min)
  - email outbox drainer — `POST /api/v1/admin/notifications/dispatch-emails` (~1 min)
  - **LP exit-request expiry** — `POST /api/v1/admin/liquidity/expire-requests` (~10 min)
  - reconciliation report — `GET /api/v1/admin/reconciliation` or `scripts/reconcile.py` (nightly; alert on drift)
  - (scheduled distributions remain **admin-triggered per property/period** — no auto-runner built.)
- Stripe **deposits** are the ONLY money rail verified live locally (Stripe CLI). Verify Stripe Connect availability in the target GCC region during VPS setup.
- **This deferred stack is the main accumulated launch risk** — owner chose to keep building money phases and verify the whole stack together on the VPS.
- **Owner decision (build plan):** finish ALL remaining phases (12, 13, 14) against **mocked providers**, then **deploy + verify everything live on the Hostinger VPS in one pass at the end**. The deferred-VPS stack now ALSO gates: **Phase 12 real email** (Resend — built behind the `email_provider` seam, console locally, verified live at the end) and **Phase 13 cron scheduling** (all crons built + unit-tested with mocks; scheduled/verified live at the end). In-app + email content/logic ship now; live delivery is an end-of-build VPS step.

---

## Deferred UI debt (logged, NOT yet fixed — clean up before launch, not in a money phase)

These are honest leftovers from Phases 1–3 verification. None block the money build; all are visible-but-cosmetic or out-of-phase:
- **Hardcoded "Ahmed" greeting** — `src/pages/InvestorDashboard.tsx:62` still says "Welcome back, Ahmed" (Phase 1 said use real profile name). (FamilyInvestment + BrokerDashboard mocks are now retired in Phases 10/11; remaining "Ahmed" strings live in SecondaryMarket*/ProShareCards mock surfaces.)
- **Virtual Cards honest-disabled (D9)** — `VirtualCardRequest` now renders a "not yet available" state across all roles (no fake issuance). The real feature (issuance/funding/spend controls) is a future phase. Broker sidebar "Reports & Analytics" / "Documents" links were **removed** (out of v1 scope); the LP/Owner dashboards still have analogous `/lp-reports`/`/lp-documents`/`/owner-*` links that may be dead — verify/clean before launch.
- **Notification toggles don't persist** — no `GET/PATCH /profiles/me/notification-prefs` endpoint; `AccountSettings` toggles are local `useState` only.
- **Avatar upload deferred** — no `POST /profiles/me/avatar` (app-storage seam not provisioned).
- **2FA / Active-Sessions buttons are no-ops** — `AccountSettings.tsx:480 "Enable"`, `:491 "View"` have no handler (should be wired or explicitly disabled).
- **Owner/Developer "View Details" / "Analytics" / "Update Progress" / "Investor Report" buttons are dead** — `OwnerDashboard.tsx:376,380`, `DeveloperDashboard.tsx:381,385,389` (no onClick).
- **Property image upload → honest 503** — `POST /properties/{id}/images` (`STORAGE_NOT_CONFIGURED`); owner create form sends no images (URLs only).
- **Owner create form can't set ownership model / rich content** — defaults `model=ready-income`; the rich `content` (timeline/scenarios/terms/SPV detail) is only populated by the seed, not the owner UI.
- **Installment invest (under-construction) is still a stub** — `InstallmentCalculator.tsx` CTA only `toast.success`es; installment plans (down payment + N dated installments + late fees) are a separate, larger phase. Lump-sum (Ready) invest is fully wired in Phase 5.
- **Reinvest-discount banner is cosmetic** — `InvestmentCalculator` reinvest path shows a 5% discount, but the server ignores all client discounts (D5). Wire or remove when the reinvest/returns flow lands in Phase 6 (no live invest path triggers it today).
- **LP dashboard** — REBUILT in Phase 9: live "Backed Assets" from `ownership_ledger`, "Realized Cash Flows" (Decision-1 labeling, no per-deal P&L), and an honestly-**disabled** "Fixed-Yield Pool" (PASSIVE) tab (rates labeled indicative, not guaranteed). No more hardcoded $ mock there. Investor withdrawals fully live from the Wallet tab.
- **✅ Estate/beneficiary/gifting IS CapiMax scope** (CORRECTED 2026-06-24 by owner; the prior "BRX / not CapiMax" note was wrong). `FamilyBeneficiaryGifting.tsx` (rendered in `FamilyInvestment`'s Estate + Gifting tabs) is currently a frontend-only mock; **CapiMax WILL build a real backend for it** in the final group (inheritance engine + legal-design step). Keep the UI; do not delete/disable as out-of-scope. Phase-10 "family gifting" on our side = the member→member unit transfer (real, built) — the estate/inheritance engine is a separate, additional build.

## HIGH-PRIORITY pre-launch compliance item (legal exposure)

- **Fabricated trust / regulatory claims must be verified or removed before any public launch:** "$50M+ AUM / 15,000+ owners / 12% returns" (`src/components/home/HeroSection.tsx:25-30`), live "$125,000 acquired…" ticker (`HeroSection.tsx:165`), and **"Regulated by Financial Services Authority"** (`src/components/layout/Footer.tsx:150`) are hardcoded marketing copy. Presenting fabricated AUM/returns/regulatory status on a live financial site is a legal/compliance risk. Owner must confirm each figure or delete it. (Audit finding #5.)

---

## Resolved decisions (locked)

| ID | Decision |
|----|----------|
| D1 | KYC provider = **Sumsub** |
| D2 | Card/payment rails = **Stripe** (Phase 4); balance changes only on verified Stripe webhook |
| D3 | **Withdrawals (Phase 7): rails = Stripe Connect (bank) + NOWPayments (crypto)**; auto-approve limit **$5000 admin-configurable** (over → review queue); no daily/weekly cap; no minimum; KYC-gated. |
| D4 | **Crypto is in v1** via **NOWPayments** (NOT "OnePayments" — corrected); Phase 4 deposits, Phase 7 crypto withdrawals. IPN signed HMAC-SHA512 in `x-nowpayments-sig` |
| D5 | **No discounts/bonuses in v1 — with TWO narrow, owner-approved reversals.** Default: standard investment is at direct price; Pronova/Nova Sukuk stay `NOT_YET_ENABLED` (not payment methods, no discount logic). **Reversal 1 — family discount** (`family_reinvest_discount_pct`, default 7.5, D14). **Reversal 2 — investor reinvest discount** (`reinvest_discount_pct`, default 5.0, Phase 14). BOTH are **admin-configurable + server-authoritative** (discounted effective unit price, units = floor(amount/effective_price); the client never computes the price). No other discount path exists. |
| D6 | `developer` = **alias of `owner`** (no new `app_role`); reconcile listing UX in Phase 3 |
| D9 | Virtual cards **deferred out of v1** (Phase 11 keeps referrals/commissions; cards UI honest-disabled across all roles — built). **Broker commission model (Phase 11) — RESOLVED:** broker→client only; commission = `broker_commission_pct` (default 10.0, admin-editable) × the **platform revenue attributable to the referred client** (Phase-5 purchase platform fee + Phase-6 rental mgmt fee), **never** a % of investment amount; DB CHECK caps it at the revenue; resale/liquidity exit fees excluded (perverse-incentive); signup-only immutable link (no retroactive adoption); accrues per revenue event, stops on exit, no clawback; rate snapshotted per accrual. |
| D8 | **Secondary market (Phase 8): admin-configurable via `platform_settings`** — price bounds (default OPEN, ref = property `unit_price`), lock-up (default 0, ref = seller's earliest acquisition), resale fee (default 1.0%, **buyer-side on top**, seller gets full gross, fee retained as revenue). |
| D10 | Fees **admin-configurable** from a `platform_settings` store (Phase 5 READs it; **editable admin view shipped in Phase 5** — owner changes rates with no deploy; defaults platform 2.5% / mgmt 1.0%). **Phase-5 fee model (FINAL, owner-corrected): platform fee charged AT PURCHASE (buyer-side); management fee is ANNUAL, deducted from distributions in Phase 6** (disclosed at purchase, no money moves at buy). Frontend's hardcoded 3%/1% replaced by backend-supplied values. |
| D11 | Contract/SPV document **generation deferred out of v1** (Phase 12 keeps notifications/comms + signed-URL download of uploaded docs) |
| D12 | **Scenario B** multi-role: many-to-many `user_roles`, active role in JWT/session, role-switcher shows only authorized roles + server-enforced; Guest = unauthenticated. Sub-decision: self-serve `investor`/`owner`, admin-approved `broker`/`liquidity_provider`/`admin` |
| D7 | **LP economics (Phase 9) — RESOLVED:** two products on one balance sheet, **never commingled** (one classified `lp_positions` row per principal). **ACTIVE** = per-deal exit buyback (LP buys a seller's instant-exit at a liquidity discount, bears risk, earns rental as new owner + resale); **PASSIVE** = fixed-APY locked pool (**deferred + hard-locked**, see open item). Liquidity fee is **seller-side** (withheld from the discounted price, retained as revenue); discount + fee from `platform_settings` (`liquidity_discount_pct` 3.0 / `liquidity_fee_pct` 2.0). Seller payout snapshot-locked at request creation; fill re-derive is band-check only. |
| D13 | **Family model (Phase 10) — RESOLVED:** real on-ledger ownership move with a **REAL/PENDING split** — REAL (KYC'd member, units in `ownership_ledger`, paid directly by Phase 6) vs PENDING (reserved in holder's ledger, recorded-only, **materializes to a real move on the member's KYC-verify**). `allocated_units` redefined as the pending cache; real units always from `ownership_ledger`. Member→member transfer reuses the Phase-8 atomic pattern (zero price). |
| D14 | **Family discount (Phase 10) — RESOLVED:** **narrow reversal of D5 for family only** — `family_reinvest_discount_pct` (default **7.5**, admin-configurable, family-scoped) applied as a **discounted effective unit price** (`unit_price × (1 − d/100)`, `units = floor(amount/effective_price)`). "Pronova 2.5%" is just a label inside it; the Pronova payment system stays disabled; D5 holds everywhere else. `family_transfer_fee_pct` default 0. |
| D-fee | **Mgmt-fee leak — CLOSED:** per-row `fee_rate` stamped on `ownership_ledger` at acquisition (originals keep their agreed rate; LP/secondary/family-acquired units carry the platform rate at acquisition). Phase-6 fee base changed from `investments`-derived to **ownership-derived** (`Σ units×unit_price×fee_rate × held_ratio`) — never a global re-derive, so no investor's agreed fee changes retroactively. |

## Still-open business decisions (resolve before the noted phase)

- **Phase 9 PASSIVE economics (owner input owed) — BLOCKING the PASSIVE engine.** Hard-locked (`lp_passive_enabled=false`); engine NOT built. Needs: **(a)** yield-funding source + reserve buffer (where the guaranteed fixed APY comes from), **(b)** written ALM rules (maturity-redemption liquidity; deployment-yield-<-APY handling), **(c)** capital/reserve adequacy, **(d) FSA license confirmation** with regulatory counsel before any live offering. Fixed APY must never render as "guaranteed"; "Regulated by FSA" stays unrendered until confirmed. Open item, not forgotten.
- **Installment-plan economics** — recurring down-payment + N dated installments + late-fee logic for under-construction properties → deferred to its own phase (the `InstallmentCalculator` stub, honest-disabled).
- **Documents / file-storage** — deferred to its own phase; needs app storage provisioned (signed-URL upload/download). All document surfaces (`PropertyDocuments`, `InvestmentCertificates`, owner Documents tab, avatar + property-image upload 503 seams) are honest-disabled until then. D11 keeps contract/SPV *generation* out of v1.
- **HIGH-PRIORITY public compliance (legal exposure)** — the "$50M+ AUM / 15,000+ owners / Regulated by Financial Services Authority" marketing claims. Owner **asserts substantiation exists** and chose to leave them as-is; each (especially the regulatory claim) **must be confirmed real before public launch** (owner's call/responsibility). See the dedicated compliance section above.
- (**D9 now resolved** — Phase 11 broker commissions built; see Resolved table.)
- (**D3, D5, D7, D8 now resolved** — see Resolved table; D7 ACTIVE done, PASSIVE the only open LP piece.)

---

## Provider config status (built; honest 503 / disabled until credentials set)

- **JWT** ✅ mechanism done (set long `JWT_SECRET` + `COOKIE_SECURE=true` in prod).
- **Stripe deposits** ✅ **test keys wired + verified live locally** (`STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET`; webhook secret from `tools\stripe.exe listen`). The ONLY money rail verified end-to-end.
- **Stripe Connect payouts** (bank withdrawals, Phase 7) ⏳ reuses the Stripe secret but **needs Connect enabled + a payout webhook endpoint** (`…/stripe-payouts`); honest 503 / "link your bank" until then. Untested live.
- **NOWPayments** ⏳ UNSET — **deposits** (`NOWPAYMENTS_API_KEY`/`NOWPAYMENTS_IPN_SECRET`) and **payouts** (`+NOWPAYMENTS_EMAIL`/`NOWPAYMENTS_PASSWORD` for JWT, + 2FA + IP-whitelist + funded balance). Built, degrade to 503; IPNs need a public URL.
- **Sumsub** ⏳ UNSET (`SUMSUB_APP_TOKEN`/`SUMSUB_SECRET_KEY`/`SUMSUB_WEBHOOK_SECRET`); KYC currently set manually via /admin.
- **Google/Apple OAuth** ⏳ UNSET. **Resend email** ⏳ UNSET (`EMAIL_PROVIDER=console` locally — links print to backend log).
- See **⚠️ DEFERRED VPS VERIFICATION STACK** above for the full list of money rails + crons awaiting live testing.

Local stack (this machine): **PropShare backend 8001, frontend 8081, admin 8001/admin** (Postgres 5433, `capimax`/`capimax`; admin login `admin@capimax.com` / `Admin123!secure`). A SEPARATE project **BRX uses 8000/8080** — don't collide. **Run the backend via the venv** (`backend\.venv\Scripts\uvicorn.exe`), not global Python (which lacks deps). Full guide: `plan/RUN_LOCAL.md`. (Prod frontend `VITE_API_BASE_URL` awaits VPS deploy.)

---

## Hard constraints (NEVER lose)

1. **Standalone Python/FastAPI backend** in `backend/`; React SPA is the untrusted client and talks ONLY to this API.
2. **Own VPS (Hostinger, EU). Supabase is fully dropped** — services AND Postgres hosting. `0001` is the source of truth; no live Supabase to diff against.
3. **Money rules** (apply to every financial endpoint from Phase 4 on): server-authoritative amounts (client never sends price/balance), integer minor units / `Decimal` never float, single atomic DB transaction with `SELECT … FOR UPDATE` + Redis lock on hot rows, **idempotent** (`Idempotency-Key` on mutations; webhooks deduped on provider id), mandatory **append-only `transactions` ledger + `audit_log`**, `ownership_ledger` for units; nightly reconciliation (`wallet == sum(ledger)`, `total_units == available + issued`); webhooks are the source of truth, never browser redirects.
4. **Nothing in the existing frontend scope is cut** — every role, page, and function ends up working for real; deferred items (cards, generated docs) degrade honestly (no fake success), never deleted.
4b. **Build the backend to FIT the existing frontend; do NOT change the frontend's design.** Inspect what each screen already presents and wire the backend/flow to it (only minimal data-source/handler rewires, no redesign).
5. **No human-in-the-loop by default** for KYC/payments/withdrawals/verification — automation is the default path; human review only as a flagged exception.
6. **Phased delivery**: each phase ends testable end-to-end; no phase starts before the previous is verified; security findings scheduled, not deferred.
7. **Acceptance bar** each phase: backend ruff+black+mypy+pytest green, frontend tsc+eslint+build green, plus the phase's security/idempotency gates.
